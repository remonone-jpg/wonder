/**
 * The child's name, put into the copy wherever `{child}` appears.
 *
 * A constant for now. It is here rather than beside the prose so the copy files
 * stay pure data: the sentences carry the placeholder, and where the name comes
 * from is a question for this file alone to answer.
 */
export const CHILD_NAME = "수호";
